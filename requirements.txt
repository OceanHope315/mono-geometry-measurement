# Core Dependencies
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
ultralytics>=8.0.0

# Additional utilities
pillow>=9.5.0
matplotlib>=3.7.0
tqdm>=4.65.0

# Optional: for GPU acceleration (uncomment if using NVIDIA GPU)
# torch-cuda  # Requires installation via conda or specific CUDA version
